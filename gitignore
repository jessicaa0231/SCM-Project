# Node modules (if you use Node.js or npm packages)
node_modules/

# Build directories (if using build tools like Webpack, Parcel, etc.)
dist/
build/

# Dependency directories (if using yarn)
.yarn/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local

# IDE or editor folders
.vscode/
.idea/

# Optional: ignore local storage for tools like Firebase emulator
.firebase/

# Optional: ignore cache folders
.cache/